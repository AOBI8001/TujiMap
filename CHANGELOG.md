# 途迹更新日志

_记录 TujiMap 正式版本的用户可见改动与关键工程修复。_

---

## 1.3.0 — 2026-09-14

### 调度与等待时间

- 修复不同 Cloudflare 请求共用异步队列的生命周期问题：共享队列只存普通元数据，各请求自行等待和发出上游调用，不再跨请求执行回调或交接 Promise。
- 增加统一高德调度入口。驾车、公交、步行、关键词搜索分别按至少 420 ms 间隔发出，同类最多 3 个在途请求；后台补图最多占用 2 个搜索在途位置，为用户输入保留余量。旧版本的热实例限速不能约束跨实例总量，本版通过共享协调器解决。
- 路线按日、路段、模式交错组成最多 12 段的批次，避免一整批只调用一种服务；每批完成就更新地图和进度，不再等所有批次全部结束才显示。
- 批量路线接入边缘缓存，保留分模式缓存有效期。再次提交相同行程不重复调用；编辑或失败重试只请求缺失、过期或变化的路段。
- 将后台批次预算限制为 28 秒、前端批次等待限制为 35 秒；仅瞬时故障允许单段重试一次，永久性参数/距离/无路线错误不重复请求。
- 增加批次墙钟耗时、排队耗时与上游耗时指标；失败显示具体原因和未完成数量，不把部分结果描述为完整行程。

### 分天和交互

- 未锁定行程按每日容量约束均衡分配，再结合地理集中度、预计游玩时长做有限交换优化；手动指定天数仍优先保留。
- 显示本日景点数量、预计游玩时长及过满提示；只有一个地点时明确告知无需生成地点间路线。
- 部分路线加载时，移除/插入操作绑定真实目的地，不再依赖成功结果在列表中的下标。
- 首批路线按全部计划地点设置视野，后续批次不反复拉动镜头。
- 搜索使用单一有界请求路径，不再无提示地叠加 SDK 搜索等待；加入搜索中、无匹配和失败反馈。
- 图片后台低优先级预热，解析结果复用；无图短缓存、下载超时清理、限制重试，避免反复争用搜索和下载资源。

### 验证

- 自动回归覆盖真实 workerd / Durable Object 的独立并发请求、跨批次限速、边缘缓存、永久错误不重试、分天容量和手动锁定。
- 正式站点、真实高德：贵阳 1 天 15 点，42/42 段完成，首段约 3.1 秒、全部约 9.9 秒；重庆 3 天 45 点，126/126 段完成，首段约 2.5 秒、全部约 22.2 秒；北京手机尺寸 7 天 35 点，84/84 段完成，首段约 3.5 秒、全部约 18.4 秒。
- 三组重复点击均未产生新路线批次。上述为单次样本，不是所有城市、网络和设备的性能承诺；手机验证为浏览器触屏/视口模拟，不代表 iOS 真机测试。
- 重庆住处和地图搜索抽样约 1.86 / 1.64 秒显示首个结果；预热后连续检查 10 张卡片均已显示图片。完整测量口径见[性能验证](docs/performance-1.3.md)。

## 🚦 1.2.0 — 2026-09-02

### 关键修复

- 路线规划由“浏览器逐段请求”改为“服务端批量规划”：典型的 7 段三模式行程从 21 次跨境 HTTP 请求减少为 1 次批量请求
- 同一批次的驾车、公交与步行请求在同一个 Worker 内统一排队和限速，修复不同 Worker 实例各自计数、实际并发突破高德免费配额的问题
- 批量接口会逐段校验、在服务端重试并返回完整结果；前端不再对每一段执行最长 4 轮超时重试，显著缩短失败时的等待时间
- 超过 21 段的复杂行程会按顺序分批处理，避免触发 Cloudflare 单次请求的上游子请求限制

### 验证

- 使用重庆 8 个连续地点、7 段路线，同时计算驾车、公共交通与纯步行共 21 段，批量接口实测 21/21 成功
- 在正式域名使用真实 Edge 浏览器选择“重庆、3 天、20 个景点”，共生成 51 段三模式路线；页面仅发出 3 个顺序批量请求且全部返回 200，三种模式均无缺段，约 40.9 秒完成

## 🖼️ 1.1.5 — 2026-09-02

### 修复

- 同一景点存在多张高德图片时优先选择稳定的 `store.is.autonavi.com` 与 `aos-cdn-image.amap.com` 来源
- 同名候选地点不再采用搜索结果中的首个模糊匹配，而是优先选择名称最接近的地点，减少错图与不稳定图片来源
- 更新图片与地点缓存版本，避免继续命中修复前已缓存的不稳定地址

## 🌐 1.1.4 — 2026-09-02

### 修复

- 识别部分网络环境下长期超时的 `aos-comment.amap.com` 景点图片，只对这一来源启用同域缓存转发
- 其余稳定的高德图片继续直连 CDN，兼顾加载可靠性、速度与 Worker 负载
- 图片转发使用原生边缘网络流并设置长期缓存，首次成功后同一景点不再重复回源

## 🧩 1.1.3 — 2026-09-02

### 修复

- 图片地址解析遇到瞬时 502、连接中断或单次超时时会自动渐进重试，不再直接把仍有图片的景点降级为首字占位
- 为每次图片地址查询增加独立超时与取消机制，避免单个第三方请求长期占住整组预加载

## 🖼️ 1.1.2 — 2026-09-01

### 修复

- 热门景点卡片会先将按名称查询的图片解析为高德 CDN 直链，再完成解码预加载，避免滑到卡片时才经历接口查询与重定向
- 图片预加载改为三路并行并带失败重试，直接图片与兜底图片同步处理；卡片渲染统一复用已解析地址
- 高德图片请求使用无来源策略，减少第三方图片防盗链或跨域策略造成的空白卡片
- 新增可缓存的图片地址 JSON 响应，后续访问同一景点无需重复等待图片搜索

## 🛠️ 1.1.1 — 2026-09-01

### 修复

- 修复重庆等多点行程并发规划时 Cloudflare Worker 偶发返回 `error code: 1101`、导致三种交通方式大量缺段的问题
- 移除 Cloudflare 部署不再需要的 Node.js 网络兼容层，路线请求统一使用边缘运行时原生网络栈
- 前端会对短暂的 Worker 500、纯文本 1101、网络中断和单次超时进行最多 4 次渐进式重试；参数或距离错误不会盲目重试
- 修复请求恰好在队列排空边界入队时可能永久滞留、令“规划中”无法结束的竞争条件
- 高德已有图片改为 HTTPS CDN 直连，按名称补图接口改为缓存重定向，不再让大批图片经过 Worker 下载和转发
- 修正图片预加载分组，直连图片使用 6 路并发，只有按名称补图才进入低并发队列

## ⚡ 1.1.0 — 2026-08-31

### 修复

- 修复 Cloudflare Workers 使用 `node:https` 兼容层时出现的高延迟与 `1101` 运行时错误，地点和路线请求改为优先使用边缘运行时原生网络栈
- 修复城市数据尚未准备完成时公交接口收到城市中文名并返回 `INVALID_PARAMS` 的问题；进入地图前确认真实城市中心与 `citycode`，服务端同时保留自动补齐逻辑
- 修复单段路线失败后继续逐段串行等待、导致整次规划持续数分钟的问题，为每段请求增加 10 秒上限并取消过期任务
- 修复热门景点图片预热与地点输入共用先进先出队列、造成住处和地图搜索长期无响应的问题
- 修复快速连续滑动热门卡片时图片尚未解码、出现空白或上一张图片停留的问题
- 修复高德地图脚本首次连接超时后直接停在空底图的问题；选城后提前预热 SDK，并在失败时自动重试一次

### 性能

- 地点输入防抖由 480 ms 缩短到 260 ms，相同查询在请求中和完成后均可复用
- 高德请求队列增加优先级，用户地点搜索和路线计算优先于按名称补图
- 驾车、公共交通和纯步行继续并行计算；每种方式内部最多同时计算两段，服务端仍按免费配额节流
- 移除前端与服务端重复的 420 ms 等待，成功路段可以更快连续写入导航栏
- 为地点、路线和景点图片接入 Cloudflare Cache API，并保留浏览器与进程内缓存
- 用户选定城市后立即预取热门 POI 与卡片图片；直链图片采用 6 路并发，按名称补图采用 2 路低优先级并发
- 图片预加载增加失败超时和解码完成判断，避免损坏资源永久阻塞卡片入口

### 交互

- 热门景点卡片未准备完成时显示“图片准备中”，准备完成后再开放连续滑动
- 路线仍按天数和交通方式渐进显示，部分上游路线失败不会清空已经生成的结果

## 📦 1.0.0 — 2026-08-30

- 发布 100 个国内城市的地图选点与多日路线规划
- 支持驾车、公共交通和纯步行真实道路导航
- 支持热门景点卡片、地点搜索、地图选择、开放与闭合路线
- 支持路线增删编辑、多住处安排和 DeepSeek 自然语言调整
- 发布响应式桌面端与移动端界面
